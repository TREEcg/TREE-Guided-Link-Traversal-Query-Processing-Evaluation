import type { PermissionSet } from './Permissions';
export declare enum AclMode {
    control = "control"
}
export declare type AclPermissionSet = PermissionSet & {
    [mode in AclMode]?: boolean;
};
