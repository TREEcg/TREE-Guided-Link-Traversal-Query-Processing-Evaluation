import type { ResponseDescription } from '../../http/output/response/ResponseDescription';
import type { RepresentationConverter } from '../../storage/conversion/RepresentationConverter';
import type { ResourceStore } from '../../storage/ResourceStore';
import type { OperationHttpHandlerInput } from '../OperationHttpHandler';
import { OperationHttpHandler } from '../OperationHttpHandler';
import type { StorageDescriber } from './StorageDescriber';
/**
 * Generates the response for GET requests targeting a storage description resource.
 * The suffix needs to match the suffix used to generate storage description resources
 * and will be used to verify the container it is linked to is an actual storage.
 */
export declare class StorageDescriptionHandler extends OperationHttpHandler {
    private readonly store;
    private readonly suffix;
    private readonly converter;
    private readonly describer;
    constructor(store: ResourceStore, suffix: string, converter: RepresentationConverter, describer: StorageDescriber);
    canHandle({ operation: { target, method } }: OperationHttpHandlerInput): Promise<void>;
    handle({ operation: { target, preferences } }: OperationHttpHandlerInput): Promise<ResponseDescription>;
}
