import type { Representation } from '../../../../http/representation/Representation';
import { BaseInteractionHandler } from '../../BaseInteractionHandler';
import type { InteractionHandlerInput } from '../../InteractionHandler';
import type { AccountStore } from '../storage/AccountStore';
/**
 * Resets a password if a valid `recordId` is provided,
 * which should have been generated by a different handler.
 */
export declare class ResetPasswordHandler extends BaseInteractionHandler {
    protected readonly logger: import("../../../..").Logger;
    private readonly accountStore;
    constructor(accountStore: AccountStore);
    handlePost({ operation }: InteractionHandlerInput): Promise<Representation>;
    /**
     * Resets the password for the account associated with the given recordId.
     */
    private resetPassword;
}
