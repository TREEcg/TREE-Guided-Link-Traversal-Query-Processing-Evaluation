import { BaseInteractionHandler } from '../../BaseInteractionHandler';
import type { InteractionHandlerInput } from '../../InteractionHandler';
import type { AccountStore } from '../storage/AccountStore';
/**
 * Handles the submission of the Login Form and logs the user in.
 * Will throw a RedirectHttpError on success.
 */
export declare class LoginHandler extends BaseInteractionHandler {
    protected readonly logger: import("../../../..").Logger;
    private readonly accountStore;
    constructor(accountStore: AccountStore);
    canHandle(input: InteractionHandlerInput): Promise<void>;
    handlePost({ operation, oidcInteraction }: InteractionHandlerInput): Promise<never>;
    /**
     * Validates the input data. Also makes sure remember is a boolean.
     * Will throw an error in case something is wrong.
     */
    private parseInput;
}
