import type { Representation } from '../../../../http/representation/Representation';
import type { InteractionHandlerInput } from '../../InteractionHandler';
import { InteractionHandler } from '../../InteractionHandler';
import type { AccountStore } from '../storage/AccountStore';
import type { CredentialsHandler } from './CredentialsHandler';
/**
 * Authenticates a user by the email/password in a JSON POST body.
 * Passes the body and the WebID associated with that account to the source handler.
 */
export declare class EmailPasswordAuthorizer extends InteractionHandler {
    private readonly accountStore;
    private readonly source;
    constructor(accountStore: AccountStore, source: CredentialsHandler);
    handle({ operation }: InteractionHandlerInput): Promise<Representation>;
}
