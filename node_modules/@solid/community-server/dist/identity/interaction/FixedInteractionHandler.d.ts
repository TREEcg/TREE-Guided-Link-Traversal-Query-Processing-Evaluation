import type { Representation } from '../../http/representation/Representation';
import type { InteractionHandlerInput } from './InteractionHandler';
import { InteractionHandler } from './InteractionHandler';
/**
 * An {@link InteractionHandler} that always returns the same JSON response on all requests.
 */
export declare class FixedInteractionHandler extends InteractionHandler {
    private readonly response;
    /**
     * @param response - @range {json}
     */
    constructor(response: Record<string, unknown>);
    handle({ operation }: InteractionHandlerInput): Promise<Representation>;
}
