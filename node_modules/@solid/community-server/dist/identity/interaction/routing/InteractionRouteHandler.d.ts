import type { Representation } from '../../../http/representation/Representation';
import type { InteractionHandlerInput } from '../InteractionHandler';
import { InteractionHandler } from '../InteractionHandler';
import type { InteractionRoute } from './InteractionRoute';
/**
 * InteractionHandler that only accepts operations with an expected path.
 *
 * Rejects operations that target a different path,
 * otherwise the input parameters are passed to the source handler.
 */
export declare class InteractionRouteHandler extends InteractionHandler {
    private readonly route;
    private readonly source;
    constructor(route: InteractionRoute, source: InteractionHandler);
    canHandle(input: InteractionHandlerInput): Promise<void>;
    handle(input: InteractionHandlerInput): Promise<Representation>;
}
