import type { SolidAccessTokenPayload, RequestMethod } from "../type";
export declare class SolidTokenVerifier {
    private dpopJtiCache;
    private issuerKeySetCache;
    private webIDIssuersCache;
    constructor();
    verify(authorizationHeader: string, dpop?: {
        header: string;
        method: RequestMethod;
        url: string;
    }): Promise<SolidAccessTokenPayload>;
}
