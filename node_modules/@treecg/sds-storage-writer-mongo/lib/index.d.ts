import type * as RDF from '@rdfjs/types';
import { Stream } from "@treecg/connector-types";
declare type SR<T> = {
    [P in keyof T]: Stream<T[P]>;
};
declare type Data = {
    data: RDF.Quad[];
    metadata: RDF.Quad[];
};
export declare function ingest(sr: SR<Data>, metacollection: string, dataCollection: string, indexCollectionName: string, timestampFragmentation?: string, mUrl?: string, maxSize?: number): Promise<void>;
export {};
