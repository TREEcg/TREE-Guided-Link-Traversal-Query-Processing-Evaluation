import type * as RDF from '@rdfjs/types';
import { Member } from '@treecg/types';
import { Collection } from "mongodb";
import { Fragment, MongoFragment } from '.';
export declare class SubjectFragmentor implements Fragment {
    readonly id: string;
    private readonly bucketProperty;
    constructor(id: RDF.Term, bucketProperty: RDF.Term);
    extract(member: Member, mongo: Collection<MongoFragment>, timestampPath?: RDF.Term): Promise<void>;
}
