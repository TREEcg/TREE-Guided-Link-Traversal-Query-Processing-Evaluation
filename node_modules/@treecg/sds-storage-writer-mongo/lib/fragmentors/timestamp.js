"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.TimestampFragmentor = void 0;
const _1 = require(".");
class TimestampFragmentor {
    constructor(id) {
        this.id = id;
    }
    extract(member, mongo, timestampPath) {
        var _a;
        return __awaiter(this, void 0, void 0, function* () {
            const timestampValue = timestampPath ? (_a = member.quads.find(quad => quad.subject.equals(member.id) && quad.predicate.equals(timestampPath))) === null || _a === void 0 ? void 0 : _a.object.value : undefined;
            if (timestampValue) {
                yield (0, _1.handleTimestampPath)([], this.id, timestampValue, member.id.value, mongo);
            }
        });
    }
}
exports.TimestampFragmentor = TimestampFragmentor;
