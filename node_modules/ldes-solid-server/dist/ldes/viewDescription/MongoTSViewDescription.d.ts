import { Store } from "n3";
import { IViewDescription } from "./ViewDescription";
export declare class MongoTSViewDescription {
    private viewDescriptionIdentifier;
    private ldesIdentifier;
    private viewIdentifier;
    constructor(viewDescriptionIdentifier: string, ldesIdentifier: string, viewIdentifier?: string);
    parseViewDescription(store: Store): IViewDescription;
    generateViewDescription(options: {
        timestampPath: string;
        pageSize?: number;
    }): IViewDescription;
    private viewDescriptionNamespace;
    private bucketID;
    private ingestorID;
}
