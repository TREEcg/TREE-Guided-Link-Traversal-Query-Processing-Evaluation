import { CacheDirectives } from "@treecg/types";
export declare function cacheToLiteral(instruction: CacheDirectives): string;
export declare type Parsed = {
    segs: string[];
    query: {
        [label: string]: string;
    };
};
export declare function parseIndex(index: string): Parsed;
export declare function reconstructIndex({ segs, query }: Parsed): string;
